---
title: "**Daily Economic Data Report**"
author: |
        | Giorgi Nikolaishvili
date: "`r Sys.Date()`"
output: 
  html_document:
    theme: flatly  
    includes:
    toc: true
    toc_float:
      collapsed: true
      smooth_scroll: true
      
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = T, warning = F, message = F, cache = T)
```

```{r include=FALSE}
library(pacman) 
p_load(dplyr, ggplot2, fredr)
```

```{r echo=FALSE}
data <- fredr(
          series_id = "SOFR",
          observation_start = as.Date(Sys.Date() - 50),
          observation_end = as.Date(Sys.Date())
        )
ggplot(data, aes(x = date, y = value, color = series_id) ) +
  geom_line() +
  labs(x = "Date", y = "Percent", color = "Series", title = "Secured Overnight Financing Rate") + 
  theme_minimal() 
```

